# migrations/superseded

Authored alternatives that were **never applied to production** and never
will be. `0019a` and `0019b` were two ways of dealing with an orphaned
signup hook; `0019c` (in `migrations/`) was chosen. Kept so the decision can
be read -- see `README_0019c_CLASSIFICATION.md`.

Nothing in this directory is ever applied by any script.
